# Copy to .env (next to appserver/docker-compose.yml) before `docker compose up`.
# OBSERVER_IP is the PRIVATE IP of observer (from `terraform output
# observer_private_ip`) so metrics/logs stay inside the VPC.
OBSERVER_IP=10.0.1.20
