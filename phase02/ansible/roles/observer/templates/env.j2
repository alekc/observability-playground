# Managed by Ansible. Grafana's external root URL (the public address the
# browser uses), so UI-built render URLs resolve to the server, not localhost.
GRAFANA_ROOT_URL=http://{{ ansible_host }}:3000/
