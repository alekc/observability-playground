---
# Resolved from phase01 outputs at runtime.
ansible_host: "{{ lookup('cloud.terraform.tf_output', 'appserver_public_ip', project_path=phase01_dir, binary_path=tofu_bin) }}"
# appserver's Alloy ships metrics/logs to observer over the private network.
observer_private_ip: "{{ lookup('cloud.terraform.tf_output', 'observer_private_ip', project_path=phase01_dir, binary_path=tofu_bin) }}"
